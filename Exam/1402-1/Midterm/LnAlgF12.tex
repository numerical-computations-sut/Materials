قراردهید
\(x_i=i\)
به ازای  
\(i=0, \ldots, N\).
فرض کنید 
\(L_i(x)\)
جند جمله‌ایهای لاگرانژ باشند.
حاصل
\(\sum_{i=0}^{N} iL_i(x)\)
کدام است؟
\vspace*{-.3cm}
\mulchoice{\(1\)}{\(x\)}
          {\(x^2\)}{\(L_N(x) - L_0(x)\)}

\begin{answer}

	\textbf{پاسخ.}
	
\end{answer}
